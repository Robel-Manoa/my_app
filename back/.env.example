# Backend environment variables
PORT=3001
DATABASE_URL=postgres://portal:portal@postgres:5432/portal
NODE_ENV=development

# Prototype persistence uses in-memory repositories.
# PostgreSQL is provided by docker-compose so the repository port can be swapped later.
